package nl.nki.responsepredictor.xgmml;

public class XgmmlGraphicGraphHelperTest {
	
	/*	@Test
	public void loadGraphicGraph() throws JAXBException, FileNotFoundException {
		


		XgmmlParserRpNetwork parser = new XgmmlParserRpNetwork();
		GraphicGraph gg = parser.loadGraphicGraph(network,true);
		ArrayList<Object> nodesEdges = (ArrayList<Object>) gg.getNodeOrEdge();
		GraphicNode node = (GraphicNode) nodesEdges.get(0);
		assertTrue(node.getId().equals("-1"));
		assertTrue(node.getLabel().equals("A"));

		ArrayList<Att> atts = (ArrayList<Att>) node.getAtt();
		Att a0 = atts.get(0);
		assertTrue(a0.getName().equalsIgnoreCase("state"));
		assertTrue(a0.getType() == ObjectType.REAL);
		assertTrue(a0.getValue().equals("NaN"));

		// TODO check for other node attributes and for the Edge object
	}*/

}
