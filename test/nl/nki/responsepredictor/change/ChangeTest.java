package nl.nki.responsepredictor.change;

public class ChangeTest {
	
	//produce list of adding changes to network which are higher than the provided score
	
	//give the score without the change
	
	
	//adding to abc model, from C to A activation, gives modelchecking value 0.6  
	
	// ?? two digits
	
	//addSingleEdge();
	
	//retrieve network
	
	//put single edge to network
	
	//calculate
	//for obs1.json and model abc.xgmml without change the modelchecking score is 0.0
	
	// with adding edge C activates B, the score becomes 0.5
	
	//

}
